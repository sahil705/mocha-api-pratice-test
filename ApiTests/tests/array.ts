let myArray = []

// 1. pop operation to remove last element of array
let number  = [1,2,3,4,5]
let popOperation = number.pop()
console.log(number) 


let tool = ["Selenium", "Postman", "Playwright"]
tool.push("Mocha")//2. insert item in array
console.log(tool);

let color = ["Red", "Green", "Blue", "Orange"]
let firstColor = color.shift()//3. remove first element from array and return that element
console.log(firstColor)


color.unshift("pink", "brown")//4. add element in  existing array in first place 
console.log(color)
console.log(color.length)

let animal = ["Dog", "Cat", "Bear", "Dear","Tiger"]

